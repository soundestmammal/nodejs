import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return (
        <div>
            This is the text that is here.
        </div>
    );
}
ReactDOM.render(<App />, document.querySelector('#root'));